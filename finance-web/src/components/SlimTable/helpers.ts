// biome-ignore lint/suspicious/noExplicitAny: <explanation>
export function getNestedValue<T>(_obj: T, _path: string): any {
  //return path.split('.').reduce((acc, part) => acc && acc[part], obj);
  throw Error('not implemented');
}
